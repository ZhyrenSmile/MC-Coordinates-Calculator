# MC-Coordinates-Calculator
Python 3


Requirements:
colorama,
ctypes
And something else just python3 -m pip install whatever it needs
ok bye
Also windows only because i forgot to add a system recognition.
